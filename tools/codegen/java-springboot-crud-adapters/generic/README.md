# Generic Java Spring Boot CRUD Adapter

通用 Java Spring Boot CRUD adapter 尚未实现。

在本 adapter 可用前：
- 禁止对非 gupo 项目使用 gupo adapter。
- 禁止低质量模板拼接式 CRUD。
- 可在用户确认后转入 `backend-java-springboot` 做生产级手写实现。
- 需要先创建新的 adapter 规格、脚本和验证用例。
